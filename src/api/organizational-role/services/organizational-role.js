'use strict';

/**
 * organizational-role service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::organizational-role.organizational-role');
